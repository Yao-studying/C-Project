#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <graphics.h>
#include <windows.h>
int main()
{
    // 1.创建界面  width:450   height:875   单位:px(像素)
    // 一个格子宽75,高125;整个界面可以同时显示6行7列

    initgraph(450, 875);

    // 根据

    // 卡屏
    Sleep(2000);

    // 结束
    return 0;
}